{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "voice-overlay",
  "description": "Only the voice overlay window receives core UI capabilities",
  "windows": ["voice"],
  "permissions": ["core:default"]
}
